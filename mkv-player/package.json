{
  "name": "matroska-remux-build",
  "version": "1.0.0",
  "description": "Build helper for the MKV remuxer wasm package @matroska-js/remux (AGPL-3.0).",
  "private": true,
  "scripts": {
    "dev": "wasm-pack build --target web && node scripts/fix-pkg-name.mjs",
    "build": "wasm-pack build --target web && node scripts/fix-pkg-name.mjs"
  },
  "license": "AGPL-3.0-only"
}
