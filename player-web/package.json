{
  "name": "matroska-player-web",
  "private": true,
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "Browser MKV player demo built on mkv-player-ui: URL box, file picker, shareable links.",
  "scripts": {
    "dev": "vite",
    "build": "bash -c 'cd ../mkv-player && npm run build' && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "mkv-player-ui": "file:../player-lib",
    "jassub": "^2.5.6"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
