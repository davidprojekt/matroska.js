; Dev-only: make PHP opcache re-check file mtimes on every request, so `podman cp`-ing updated
; app code in (dev/sync.sh) takes effect immediately instead of after opcache.revalidate_freq.
opcache.revalidate_freq=0
