/node_modules
# Vite build output (emitted into the app root by @nextcloud/vite-config).
/js
/css
/dist
/assets
# Bundled ffmpeg.wasm core, copied from player-lib's build (npm run setup:ffmpeg); not committed.
/ffmpeg
# Staged deployable app for the dev container (scripts/assemble.sh).
/deploy
*.tsbuildinfo
